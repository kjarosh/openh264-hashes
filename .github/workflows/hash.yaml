name: Hash

on:
  schedule:
    - cron:  '5 1 * * *'
  workflow_dispatch:

jobs:
  hash:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run
        env:
          GH_TOKEN: ${{ github.token }}
        run: ./openh264_hash.py
